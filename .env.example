# Local dev overrides. In-cluster these are injected by Helm/k8s instead.
AGENT_NAME=my-agent
AGENT_LOG_LEVEL=DEBUG
# AGENT_MODEL=claude-sonnet-5           # uncomment to give it an LLM brain
# Nested connector config uses __, e.g.:
# AGENT_JIRA__BASE_URL=https://your-org.atlassian.net
