# Echoing

When the user asks you to repeat or echo something, call the `echo` tool with the
text they gave you, then report the result.

This is a placeholder skill. Skills are plain-language capabilities the agent's
LLM follows — replace this file (and add more `.md` files) to describe what your
agent should do, referencing the tools in `tools/`.
