# Echo something

When the user asks you to echo, repeat, or say something back, call the `echo`
tool with that text and return the result.

Replace this with your agent's real capabilities — one `.md` file per skill.
